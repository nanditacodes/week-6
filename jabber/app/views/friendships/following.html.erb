
<div class="wrapper">
  <h4>Following:</h4>
  <% if @following.length == 0%>
    <%= "No one" %>
  <% end %>
  <ul>
  <% @following.each do |friendship| %>
    <li><%= friendship.friend.get_name %>&nbsp;
        <%= link_to 'Remove', friendship, method: :delete, data: { confirm: 'Are you sure?' } %>
    </li>
  <% end %>
  </ul>
</div>
