<p id="notice"><%= notice %></p>
<div class="message-box"
  <p>
    <strong>
      <%= @friendship.user.name || @friendship.user.email %> is now following <%= @friendship.friend.name %>
    </strong>
  </p>
  <%= link_to 'Back', root_path %>
</div>
